// do not change anything inside this file; That may lead to un testable output
apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = 'solution'
mainClassName = 'com.tigerit.exam.CommandLineApp'

run {
    standardInput = System.in
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.8'
}